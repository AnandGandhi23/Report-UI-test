export interface ReportRequest {
    startDate: string,
    endDate: string
}

export interface ReportRequestByYear {
    year: number
}

export interface flterDropdown {
    label: string,
    value: string
}